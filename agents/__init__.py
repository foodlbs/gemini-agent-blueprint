"""v2 LlmAgents — one definition per node group per DESIGN.v2.md §6.

Each module here re-exports the agent objects the Workflow in agent.py
references. Bodies are real-ish skeletons; full per-node implementation
arrives in subsequent turns per §6.x."""
